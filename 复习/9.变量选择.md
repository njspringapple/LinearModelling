
- **建模目的**：
	- 可以**描述**模型中的变量如何影响结果
	- 可以**预测**未来值/**估计**目标变量均值
	- 可以**估计**参数
	- 外推到已有数据范围之外（泛化）
	- 了解了变量行为后，可以通过改变输入变量的值来控制过程
	![[2f793840-df65-45ad-956c-cc5bcf4444de.png|690]]

- **偏差-方差 权衡**
	- 加入更多变量通常能 **改善训练集拟合**，但可能 **降低新数据预测能力**吗，变量选择本身也会引入不确定 ！！！！！
	- Error = $Bias^2 + Variance + Noice$
	- 模型太简单：高偏差，欠拟合
	- 模型太复杂：高方差，过拟合
	- 好模型：**在偏差和方差之间折中**

![[Pasted image 20260926020204.png|550]]
- 左侧：随着模型变复杂，训练数据集上**误差下降**，测试集上**误差也下降**
- 右侧：随着模型变复杂，训练数据集上误差下降，测试集上**误差反向上升**
- **因此，不是训练数据集上无限制增加模型复杂度，选择合适的模型复杂度，否则导致模型过拟合，对于”未知“数据的推断误差增加**


- **模型优度指标 / Masse fuer die Modellguete**
	- 普通 $R^2$：$R^2=1-\frac{SSE}{SST}$ ----- **问题：加入变量后 $R^2$ 不会下降**，---- **$R^2$ 是模型所有参数共同解释了 Y（因变量），但是不意味着每个变量都显著！！**
	- 调整 $R^2$：$R^2_{adj}=1-\frac{SSE/(n-p-1)}{SST/(n-1)}$ ----- **标准化后，可以用于不同模型的拟合效果对比** -- **如果调整后的 $R_{adj}^2$ 明显小于 $R^2$ ，说明模型参数至少有一个参数不显著**
		- **计算方法**：
			- $R^2_{adj}=1-\frac{(n-1)}{(n - p')}(1 - R^2)$
	- **AIC**：$-2\ell(\widehat{\theta})+2k$
		- $\ell(\widehat{\theta})$ - **最大对数似然**
			- 似然函数：$L(\theta) = \prod_{i=1}^n p(x_i|\theta)$       -- $\theta$ 是模型参数，例如 $\beta_0,...,\beta_{p}$ **含义： 在参数为 $\theta$ 情况下，观察到现在这组数据的概率是多少**
			- 取对数，$\ell(\theta) = ln L(\theta) = \Sigma_{i=1}^n ln p((x_i|\theta))$
			- 求最大值：求导，$\partial \ell(\theta) / \partial \theta = 0$
			- 求解出来的 $\hat{\theta}$  就是 最大似然估计值，然后代回对数似然 $\ell(\theta)$ 得到 AIC 里面第一项
		- $k$ - **模型参数个数**(含截距，含误差项，即 k = p(建模参数个数) + 1（截距） + 1（误差）)
		- **数值越小越好**
		- **计算方法**：
			- $AIC = -2 · loglik(model) + 2(p′ + 1)$， 其中 p‘ = （p+1） 是建模参数数量加1
			- $AIC = nln(SSE) + 2(p' + 1) - nlog(n)$
			- AIC(model) -- R
	- **BIC**：$BIC=-2\ell(\widehat{\theta})+\log(n)k$  --- **BIC 对复杂度惩罚通常比 AIC 更强，倾向于选择参数更小的模型**
		- **贝叶斯信息准则**（Bayesian Information Criterion）
		- $-2\ell(\widehat{\theta})+k \cdot ln(n)$
			- n - 样本数量
		- **数值越小越好**
		- **计算方法**：
			- $BIC = -2 · loglik(model) + ln(n)(p′ + 1)$， 其中 p‘ = （p+1） 是建模参数数量加1
			- $BIC = nln(SSE) + ln(n)(p' + 1) - nln(n)$
			- BIC(model) -- R

- **模型选择基本原则**
	- 有交互项，但是不能没有主效应：
		- $y \sim x_1 + x_1:x_2$  - 有主效应
		- $y \sim x_1:x_2$ - 缺乏主效应，错误！！
	- 自变量之间高度相关，导致多重共线性（Multikonllinearitat)
		- 在做模型选择的时候，先看候选变量的相关性
	- 分类变量要么不加入模型，要么全部加入
	- **交叉验证**：用预测能力来评估模型好坏
		- 把数据切分为 K 份
		- 进行 K 轮训练，每轮训练选用 K - 1 份数据做训练数据集，一份数据进行预测，得到 误差 SSE
		- 把 K 轮 SSE相加，得到 CV = SSE_1 + ... + SSE_k
		- **CV 越小的模型，预测能力越好**
		- **即可以 从候选变量里挑出一个子集模型 ，也可以确认模型情况下，评估一个模型的”泛化误差“**

```r
# 构造测试数据
set.seed(1)
n <- 100
x1 <- rnorm(n); x2 <- rnorm(n); x3 <- rnorm(n)
y  <- 1 + 2*x1 + 3*x2 + rnorm(n)
df <- data.frame(y, x1, x2, x3)

# 待比较的若干"模型设定"
models <- list(
  m1 = y ~ x1,
  m2 = y ~ x2,
  m3 = y ~ x1 + x2,
  m4 = y ~ x1 + x2 + x3
)

# K 折交叉验证：返回每个模型的平均 MSE
cv_mse <- function(formula, data, K = 10) {
  n <- nrow(data)
  # 创建 K 个 数据集合 folds，数组100个元素，每个元素值为 1-K 之间
  folds <- sample(rep(1:K, length.out = n))   # 随机分配到 K 折
  mse <- numeric(K)
  for (k in 1:K) {
    # 抽取测试数据集 和 训练数据集
    test  <- data[folds == k, ]
    train <- data[folds != k, ]
    # 用训练数据集 拟合，得到模型
    fit   <- lm(formula, data = train)
    # 用测试数据集 和 模型，进行预测，返回预测后的拟合值 （y_hat)
    pred  <- predict(fit, newdata = test)
    # 计算 MSE
    mse[k] <- mean((test$y - pred)^2)          # 测试集 MSE
  }
  mean(mse)                                     # K 折平均
}

# 对每个模型跑 CV
sapply(models, cv_mse, data = df)
#      m1       m2       m3       m4 
#9.100678 4.143192 1.070570 1.050685
# 很明显，交叉验证结果是 m4 效果最好
```


- **变量选择方法**：采集导一堆维度数据，怎么知道用哪些？
	- **从候选变量里挑出一个子集模型**
	- **穷举法（Auswahl nach einem Kriterium）**：K个变量，$2^K$ 个可能模型，分别训练，挑选 AIC/BIC 小的（变量多了算力要求太高）
		- 计算量大，但是可以找到最优
	- **前向选择（forward）**：空模型开始一个个加参数，每加一个参数，选择 $R^2$ 最高的 --- **加进去之后，永远不会被删除，速度快，同理 backward 也是，减少之后不允许加回来**

```r
library(MASS)
# 先根据预设模型，制造100个观察到的数据，每个数据三个参数
set.seed(1)
n <- 100
x1 <- rnorm(n); x2 <- rnorm(n); x3 <- rnorm(n)
y <- 1 + 2*x1 + 3*x2 + rnorm(n)   # 真模型只含 x1, x2
df <- data.frame(y, x1, x2, x3)
# 前向选择，从截距开始，到所有参数截至
null_model <- lm(y ~ 1, data = df)
full_model <- lm(y ~ ., data = df)
# 从没有参数的空模型（只有截距）开始，scope 是备选的模型池
stepAIC(null_model, scope = list(lower = null_model, upper = full_model), direction = "forward")
```

```r
# 初始只含截距，AIC = 249
Start:  AIC=249.03
y ~ 1

# <none> 当前模型（只含有截距），AIC = 249
# 尝试加入 x1，x2，x3，看看哪个最低
# 加入x2，AIC 最低，所以第一轮，选择 x2
       Df Sum of Sq     RSS    AIC
+ x2    1    783.89  398.69 142.30
+ x1    1    300.95  881.64 221.66
<none>              1182.59 249.03
+ x3    1      0.00 1182.59 251.03

Step:  AIC=142.3
y ~ x2
# 第二轮，在已经有x2的基础上，加入 x1，x3尝试
# 加入x1最低，所以这轮选择 x1

       Df Sum of Sq    RSS     AIC
+ x1    1   301.915  96.78   2.725
<none>              398.69 142.302
+ x3    1     1.943 396.75 143.814

Step:  AIC=2.72
y ~ x2 + x1

# 第三轮，当前 AIC = 2.7249
# 加入 x3 后，AIC 升高到 3.5
# 所以不选择，结束
       Df Sum of Sq    RSS    AIC
<none>              96.778 2.7249
+ x3    1     1.156 95.622 3.5233

# 总结，模型为 y ~ x2+x1
Call:
lm(formula = y ~ x2 + x1, data = df)

Coefficients:
(Intercept)           x2           x1  
      1.055        2.939        1.944
```

- **后向淘汰（backward）**：从有所有参数的模型开始，逐步去掉参数，先去掉1个，去掉 $R^2$ 最高的，直到 AIC 不再下降
	- 前向和后向计算量小，但是不能确保找到全局最优参数集合，而且不同起点和方向，结果可能不稳定

```r
library(MASS)
# 先根据预设模型，制造100个观察到的数据，每个数据三个参数
set.seed(1)
n <- 100
x1 <- rnorm(n); x2 <- rnorm(n); x3 <- rnorm(n)
y <- 1 + 2*x1 + 3*x2 + rnorm(n)   # 真模型只含 x1, x2
df <- data.frame(y, x1, x2, x3)
# 前向选择，从截距开始，到所有参数截至
null_model <- lm(y ~ ., data = df)
full_model <- lm(y ~ 1, data = df)
# 从没有参数的空模型（只有截距）开始，scope 是备选的模型池
stepAIC(null_model, scope = list(lower = full_model, upper = null_model), direction = "backward")
```

```r
# 初始设置全模型 x1+x2+x3, AIC = 3.52
Start:  AIC=3.52
y ~ x1 + x2 + x3

# 第一轮，当前 AIC = 3.5
# 尝试减去其它x，减去x3后，AIC降低最大
# 这一轮选择 ”减去“ x3

       Df Sum of Sq    RSS     AIC
- x3    1      1.16  96.78   2.725
<none>               95.62   3.523
- x1    1    301.13 396.75 143.814
- x2    1    785.92 881.54 223.650

Step:  AIC=2.72
y ~ x1 + x2

# 第二轮，当前AIC = 2.725
# 尝试减去 x1 或者 x2， AIC都上升
# 所以，结束，不再减去任何参数

       Df Sum of Sq    RSS     AIC
<none>               96.78   2.725
- x1    1    301.91 398.69 142.302
- x2    1    784.86 881.64 221.661

# 总结，模型为 y ~ x2+x1

Call:
lm(formula = y ~ x1 + x2, data = df)

Coefficients:
(Intercept)           x1           x2  
      1.055        1.944        2.939
```

- **逐步回归 (both)**：向前或者向后只能添加或者删除，不能返回，both是可以添加后反悔删除掉

```r
library(MASS)
# 先根据预设模型，制造100个观察到的数据，每个数据三个参数
set.seed(1)
n <- 100
x1 <- rnorm(n); x2 <- rnorm(n); x3 <- rnorm(n)
y <- 1 + 2*x1 + 3*x2 + rnorm(n)   # 真模型只含 x1, x2
df <- data.frame(y, x1, x2, x3)
# 前向选择，从截距开始，到所有参数截至
null_model <- lm(y ~ 1, data = df)
full_model <- lm(y ~ ., data = df)
# 从没有参数的空模型（只有截距）开始，scope 是备选的模型池
stepAIC(null_model, scope = list(lower = null_model, upper = full_model), direction = "both")
```

```r
# 初始只含截距，AIC = 249
Start:  AIC=249.03
y ~ 1

# <none> 当前模型（只含有截距），AIC = 249
# 尝试加入 x1，x2，x3，看看哪个最低
# 加入x2，AIC 最低，所以第一轮，选择 x2
       Df Sum of Sq     RSS    AIC
+ x2    1    783.89  398.69 142.30
+ x1    1    300.95  881.64 221.66
<none>              1182.59 249.03
+ x3    1      0.00 1182.59 251.03

Step:  AIC=142.3
y ~ x2

# 第二轮，在已经有x2的基础上，加入 x1，x3尝试
# 加入x1最低，所以这轮选择 x1

       Df Sum of Sq     RSS     AIC
+ x1    1    301.91   96.78   2.725
<none>               398.69 142.302
+ x3    1      1.94  396.75 143.814
- x2    1    783.89 1182.59 249.029

Step:  AIC=2.72
y ~ x2 + x1

# 第三轮，
# 尝试加入 x3 和 减去 x1，x2，结果比较，AIC都上升
# 所以结束
       Df Sum of Sq    RSS     AIC
<none>               96.78   2.725
+ x3    1      1.16  95.62   3.523
- x1    1    301.91 398.69 142.302
- x2    1    784.86 881.64 221.661

Call:
lm(formula = y ~ x2 + x1, data = df)

Coefficients:
(Intercept)           x2           x1  
      1.055        2.939        1.944
```