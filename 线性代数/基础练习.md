
### 向量矩阵变换/线性组合


1. $x$ 一般指 （      ）向量；$x^T$ 一般指（        ）向量
2. **变换视角**：$Ax$ 可以理解为 （   ）向量 x，通过矩阵 A 进行变换，$x^TA$ 可以理解为（     ）向量 $x^T$ 被矩阵 A 进行变换，利用转置性质，$(AB)^T = B^TA^T$，$x^TA$ 可以写为（               ）
3. **线性组合视角**：$Ax$ 可以理解为 矩阵A的各个（          ）向量，通过向量 $x$ 的各个成员 $x_i$ 进行线性组合。$x^TA$ 可以理解为 矩阵 $A$ 的各个（          ）向量，通过 向量 $x$ 的各个成员 $x_i$ 进行线性组合
4. $A = (a_1,...,a_n)$, $x = (x_1,...,x_n)^T$ ，$Ax$ = (                                                            )
5. $A = (a_1,...,a_n)^T$,$x = (x_1,...,x_n)，xA =$ (                                                              )
6. $A \in R^{m \times n}, B \in R^{n \times p}$，$A \cdot B$ 可以理解为 矩阵 A 对 矩阵B的 （                  ）向量进行批量线性变换。也可以理解矩阵 B 对矩阵 A的（                 ）向量进行批量线性变换
7. $A \in R^{m \times n}, B = (b_1,...,b_p)$，如果 A 作为变换矩阵，则 $A \cdot B  =$ (                           )
8.  $A = (a_1,...,a_m)^T, a_i \in R^n, B \in R^{n \times p}$，如果 B 作为变换矩阵，则 $A \cdot B  =$ (                           )       **-- 注意，矩阵向量乘法的维度要正确！**
9.  矩阵 $A \in R^{m \times n}$,     如果列向量全部线性无关，则：rank(A) = (             )，ker(A) = （           ）；如果 rank(A) = r，则矩阵A的最大线性无关组包含 （            ）向量，零空间元素数量 = （                   ）
10. 矩阵 $A \in R^{m \times n}$ 可逆，意味着 ker(A) = (                 )，理解为没有 ”非零向量被 A 映射为 零向量“，此时，det(A) (         ) 0，表示该线性变换对**体积的缩放因子**
11. 由矩阵 A 的线性无关向量张成的空间，称为（              ），书写为（                       ），也可以写作（                               ）
12. Ax = b，可以理解为寻找一个向量 x，可以被矩阵 A 变换成 b，如果对于任意的 b 都成立， 这要求 A（                ），此时 det(A) (          ) 0，
13. Ax = b, 看作是线性方程组，如果无解，即 b 不在 (                   )  中，此时，如果 $A \in R^{m \times n}$，有 m (           ) n，这种属于（              ）方程

































### 解答


14. 列；行
15. 列；行；$(A^Tx)^T$
16. 列；行
17. $x_1a_1+x_2a_2+\cdots+x_na_n$
18. $x_1a_1^T+x_2a_2^T+\cdots+x_na_n^T$
19. 列；行
20. $(Ab_1,Ab_2,\dots,Ab_p)$
21. $\begin{pmatrix}a_1^TB\\a_2^TB\\\vdots\\a_m^TB\end{pmatrix}$
22. $n$；$\{0\}$；$r$ 个；$n-r$
23. $\{0\}$；$\neq$
24. 列空间；$\operatorname{Col}(A)$；$\mathcal R(A)$
25. 可逆；$\neq$
26. $\operatorname{Col}(A)$；$>$；超定方程